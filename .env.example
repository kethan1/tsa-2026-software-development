GEMINI_API_KEY="MY_GEMINI_API_KEY"
APP_URL="MY_APP_URL"
# Exposed to the browser by Vite (must keep the VITE_ prefix). The Speech tab
# calls Gemini directly from the client, so this key ships in the bundle.
VITE_GEMINI_API_KEY="MY_GEMINI_API_KEY"
